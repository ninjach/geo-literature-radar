{
  "name": "geo-literature-radar",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "refresh": "node server.js --refresh-once",
    "export:static": "node server.js --export-static",
    "build:pages": "node server.js --build-pages",
    "build:pages:refresh": "node server.js --build-pages --refresh",
    "validate": "node server.js --validate-static",
    "test": "node server.js --self-test"
  },
  "engines": {
    "node": ">=20"
  }
}
